# Two-channel reward on sunbeam sleep — queued inputs (2026-09-24)

Filed from a Professor-thread handover (2026-09-23, written at the
owner's request; relayed 2026-09-24). A relay, not a kickoff: nothing
here starts until the owner raises it in the Experiments session. The
framing's home is the Professor thread —
`~/ai/professor/notes/for-experiments-two-channel-reward.md` and
`~/ai/professor/reading/two-channel-reward.md` — this file records the
queue item and what it touches in this directory.

## Her stated intent (via the handover)

Build a two-channel / floor-style reward treatment on the sunbeam
implementation now; complete Gen 2 (hidden needs); carry the approach
into Gen 3 (free time), replacing the current Gen 3 reward idea, which
she judges functionally similar to the current sunbeam floor.

## The question, in this directory's terms

The "weight tweaks inert, need floor decisive" result is F-047 (a
relief differential worth one tick per nap never moved placement;
price 7/10/15 never left the seed spread) plus F-050 (beam seeking
survives PPO once the tile changes a nap's outcome — the shallow-ground
floor, spec 056). Three formalizations fit that pair and make
different claims: the hard floor as shipped (threshold-driven, no
exchange rate below it), a convex homeostatic drive (one scalar
reward, curvature does the floor's job — Keramati & Gutkin 2014), and
a Lagrangian cost channel with a learned multiplier (RCPO/CPO; the
multiplier reads out "what sun is worth in chow"). (1) vs (2) tests
whether the discontinuity mattered; (2) vs (3) is the two-channel
question proper.

## What Experiments settles when she says go

- The observable: sunbeam placement alone, or the whole need profile
  (does fixing sun starve something else) — F-009 dimensions declared
  either way.
- Under (3): does the multiplier stabilize, and does its value read as
  a roster-personality quantity (per-seat multipliers vs the trait
  sheets)?
- Whether the current Gen 3 free-time reward idea reduces to (1); if
  so, (2) or (3) is the actual change, not a re-skin.
- Welfare-gate compatibility: what the frozen two-layer gates certify
  (certification-gate philosophy: 0.002 is a noise reading, never a
  pass bar) under a reshaped reward; any change to gates or budgets is
  the owner's word by the §Ownership list.

## Standing constraints it lands inside

Gen-order is ruled: Gen 2 (hidden needs) → Gen 3 (free time) → LLM
seats (owner 2026-09-20, #392 deferred behind them). A sunbeam-reward
treatment re-opens F-047/F-050 territory and F-052's deafening prior
(re-run declared before the Gen 2 cert). Doctrine rule 9 applies if
minds are retrained under a new objective: frozen models cannot answer
a reprice. Comprehension of whatever gets built goes back to the
Professor thread; decisions and records stay here.
