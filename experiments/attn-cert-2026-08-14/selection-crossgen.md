# Candidate B: the cross-generation roster (registered 2026-08-14)

**Status: FROZEN on registration (owner: "let's run the battery on
this seating").** A second candidate under the SAME certification
protocol as `selection.md` — its gate structure, anchors, instrument
(D-001), and re-anchorings (D-002: distress-age ≤ 150; D-003:
seat-paired welfare, −0.006) apply verbatim. Deviations append here.

## The seating map (pinned)

| seat | kitty | model | artifact | sha256 |
|---|---|---|---|---|
| 1 | Miso | attn-A1-s1 "the cuddler" | v3 | `d8e31021…` |
| 2 | Biscuit | **e004-a1-s2 "the founding purr culture"** | v2, deployed | (policies/README.md) |
| 3 | Pumpkin | attn-A1-s3 "the doter" | v3 | `dfef0ec2…` |
| 4 | Kittybear | attn-A1-s3 "the doter" | v3 | `dfef0ec2…` |

Mixed ARTIFACT VERSIONS by design — spec 030 serves v2 beside v3
byte-identically; that machinery is itself under test here.

## Registered rationale and priors (screen seen before this freeze —
declared, not hidden)

The cross-generation probe (2026-08-14, probe band, 5×10k): roster
mean 94.90 (nominally best measured); the previous generation
GROOMS BACK (~8.7% of decisions, aimed at the doters) — it carries
the trained responder culture (answer WantBath with grooming) that
the attention lottery lost, closing the doters' gift economy;
doters +0.1–0.3 vs the all-attention roster; top friendship = the
two far-ping purrers (s1 + PREV); no isolate. The candidate keeps a
beloved incumbent serving and demonstrates generational diversity
as a character axis.

## Gates (inherited verbatim; quantities restated)

G1 (already green for all four artifacts) · G2a bounds · G2b mda ≤
150 · G2c hard floor ≥ scripted 0.9058/0.9060 AND paired team Nash ≥
incumbent − **0.005** (same declared budget; parity read at 0.002) ·
G2d every seat ≥ incumbent same-seat − 0.006 (Biscuit expects ≈ 0:
same model, same seat) · G3 stress bar 225, ≤1/30 exceedances,
low_share ≤ 5%. Incumbent and scripted anchor legs REUSED from the
candidate-A battery (same bands, instrument, seeds — candidate-
independent by construction).

## Decision rule

Both candidates certified → the owner chooses the seating; this doc
confers eligibility, never precedence. Failure handling as in
selection.md: stop, report, owner decides.
