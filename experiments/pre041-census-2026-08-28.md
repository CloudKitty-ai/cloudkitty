# Pre-041 deploy census (2026-08-28)

The "pre" half of the 041 rollout's pre/post pair (fog timeline step 1;
house rule: re-baseline before deploy). Taken against the RUNNING
pre-041 world after #316–#320 merged but before any deploy. Raw:
`attn-cert-2026-08-14/results-raw/need-latency-763947.json`
(uncommitted, provenance-stamped).

## Needs latency (`need_latency.py --live 9.5 5`)

Window: ticks 763,947–764,760 (813 ticks, 115 polls @5s).
`bad_gaps` 0 on every (seat, need); happiness residual 0.0.

Demand price (happiness pts): Miso 5.14 · Pumpkin 5.60 · Kittybear
4.85 · Clementine 5.71 · **Biscuit 10.06**.

Biscuit detail — the second window the design call asked for, and it
confirms the 2026-08-26 baseline at a different hour and a HOTTER
reading: eat mean 17.36 (was 15.64), 3 armed excursions with **max
latency 78 ticks** (was 34), time>30 = 14.4% of the window; drink 4
excursions (p50 9.5); sleep now arms too (1 excursion, 14 ticks);
play still never piles up (79 reliefs, mean 4.95, 0 excursions).
The eat/drink/sleep-not-play shape of the gap is stable across both
windows; magnitude varies with the hour (9.06 → 10.06).

Elsewhere: quiet. Scattered single excursions (Pumpkin eat 25,
Kittybear/Miso bath pairs, Clementine cuddle 14.1) — all one-offs,
nothing sustained.

## Clustering (`nn_distance.py --live 50 10`)

Ticks 764,699–765,406, 250 samples: **cheb median 1.0 / mean 1.76 /
p90 4 / contact 0.684** — matches the pinned step-0 baseline
(median 1.0 / mean ~1.9 / p90 4–5 / contact 0.66–0.67).

## Use

- Post-deploy census re-runs both instruments on the same dials;
  deltas read against THIS file, not the 2026-08-26 baseline.
- Pre-declared (timeline step 1): zero incumbent rest scenes
  post-deploy is EXPECTED; soak watches welfare/watchdog. A few odd
  incumbent rest scenes = the untrained-logits-at-select mechanism's
  signature, not a defect.
- Biscuit 3.0 design call: the second-window requirement is met;
  both windows agree on the gap's identity (food, not play).
