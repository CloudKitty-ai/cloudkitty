[package]
name = "bc-collect"
version = "0.1.0"
edition = "2021"
publish = false
description = "BC dataset collector for exp-001: behavior-driven rollouts recorded as (observation, mask, applied-action label, team reward) npy shards."

[dependencies]
cloudkitty-core = { path = "../../../crates/cloudkitty-core" }
cloudkitty-rl = { path = "../../../crates/cloudkitty-rl" }
serde_json = "1"
sha2 = "0.10"
toml = "0.8"

# Standalone on purpose: not a member of the product workspace (see
# experiments/README.md).
[workspace]
