# The exp-004 grid: 15/15 healthy, every hypothesis lands

**2026-08-09.** The full registered grid (A0 ×5, A1 ×5, D1 ×5; 20M
ticks each; frozen prereg, engine `f24e458`, stamp `412d00e2…`) ran in
the dedicated worktree, completed without a single §9.6 stop-rule halt,
crash, or fallback, in ~3 hours wall (15 concurrent, one core each).
Evaluation: the four-shape sweep (`trainer/run_eval_v4.py`, exp-002's
harness with the frozen bands — eval 870001+, stress 880001+),
evaluate-once ledger enforced; verdicts by `trainer/verdicts_v4.py`
(the frozen §9 gates, mechanically applied). Byte copies of the
per-run eval JSONs: `eval-2026-08-09/`; verdict table:
`verdicts.json` (committed beside this doc).

## The verdict table

Every candidate passes H4 (welfare ≥ baseline + 0.02 on the deployment
shape) AND H5 (the settled §9.2 gate — zero incident-rate failures,
zero low_share breaches, zero floor touches; worst `max_distress_age`
anywhere in 1,350 stress runs: 405, one incident, within tolerance).

Top of the table (Δ = subject − paired scripted baseline, shape iii):

| run | welfare | Δiii | Δi (mixed) |
|---|---|---|---|
| **A1-s2 (selected)** | 0.9500 | **+0.0440** | +0.0083 |
| A0-s3 | 0.9488 | +0.0429 | +0.0075 |
| A0-s5 | 0.9487 | +0.0428 | +0.0071 |
| … all A0/A1 within +0.0410–0.0440 … |
| D1 (ineligible) | 0.928–0.938 | +0.022–0.032 | −0.001–+0.004 |

**Selection: A1-s2** by the registered rule (eligible arms ranked by
shape-iii welfare). Honesty note: A0-s3 trails by 0.0011 — inside the
derived 0.0020 margin, so the top is a statistical tie; A1's shaping
neither helped nor hurt measurably (the on/off arms interleave through
the whole table). D1's diagnostic answer: **the channel needs no
scripted anchor** — self-play arms kept it alive without live
demonstrators, and D1's mixing bought only its usual welfare cost.

## Certification of A1-s2 (the frozen battery)

- **Channel-alive (§9.4): PASS** — 169.6 non-Silent/1k in company
  (floor 0.5), `h1_probe`, eval band. Composition: **Purr 169/1k,
  want-kinds 0.001–0.04/1k.** This is not collapse — it is grounded
  legality working on healthy cats: a want-meow is only legal at need
  ≥ 30, and this policy holds every need's p99 under ~30, so the
  asks are silent *by law*; Purr's grounding (happiness) is nearly
  always satisfied at 0.95 welfare. The channel that remains is a
  contentment hum.
- **H2 (response existence): PASS, emphatically** — GroomKitty
  **93.3 actor-ticks/1k** (74,621 ticks in the 800k census) against
  the registered floor of *any* — v3's tick-level truth was 0, the
  scripted re-baseline runs 5.9/1k. Mutual grooming is a pillar of
  the trained behavior, ~16× its demonstrators. The registered
  heard-vs-unheard direction is **unmeasurable at this generation's
  silence level** (~2 WantBath asks in 800k ticks — nothing to
  condition on) and is reported as such, not claimed: the
  demonstrated response generalized past its trigger; policies groom
  constitutively rather than on request.
- **H3 (priced presence buys duration): PASS** — mean contact run
  **4.27** (p50 5) against the registered ≥ 3.5 and the scripted
  baseline's structurally-pinned 3.0. At drip 3, a 4.27-tick contact
  pays ≈ 12.8 against the mean need of 11.6 — **the policy stays
  almost exactly as long as honest pricing pays**, which is the
  cleanest possible reading of the dial-pricing thesis.
- Behavioral economy shift (reported): cosleep serviced 144.7/1k
  (scripted B: 6.3) — 23×; rest duets nearly abandoned (0.5/1k vs
  81.9) — the policies traded the duet economy for cosleep +
  cross-currency grooming. FR-019 herding: ~1 WantBath episode per
  seed — herding is a scripted-world phenomenon; nothing to report
  in policy company.

## Hypothesis scoreboard (all registered, all resolved)

- **H1 SUPPORTED** (clone stage): 104.66 meows/1k vs floor 0.5.
- **H2 SUPPORTED** at its registered floor (GroomKitty > 0, by 5
  orders beyond); direction unmeasurable, reported honestly.
- **H3 SUPPORTED**: 4.27 ≥ 3.5.
- **H4 SUPPORTED**: 15/15 over the +0.02 bar; selected +0.0440.
- **H5 SUPPORTED**: 15/15 pass the settled gate (contrast exp-003's
  0/9 under the overtight one — the gate redesign and the healthy
  cohort are both real).

## Deviations (appendix entries filed)

- The channel probes chain episodes past the 2k horizon on derived
  sub-seeds (band-seed × 100 + episode) — disjoint from every
  declared band by magnitude; filed as deviation, not silently done.

The selected candidate is `artifacts/A1-s2/A1-s2.ckpolicy` (sha in
`verdicts.json` runs' manifests). Deployment remains gated Product-side
(client `MEOW_TEXT` + generation-3 re-certification — this candidate is
the input to exactly that re-certification).

## Addendum (2026-08-09, owner's question): the channel is dormant, not dead

Stress-world channel census (A1-s2, family-02 roster-5 — its hardest
shape — 10 seeds 880001–010 × 20k, all-policy): want-meow rates rise
with need occupancy (want_eat 0.10/1k, want_play 0.08, want_cuddle
0.05 vs 0.001–0.04 on the served world), and WantCuddle fires in
~25% of its cooldown-limited legal windows (≈50 asks into ≈2,000
above-threshold ticks). Purr falls 169 → 33.5/1k as happiness falls.
The announce behavior survived PPO intact — the policy simply keeps
itself out of the states where asking is legal. If the world pushes
needs up, the asks return without retraining; and the thresholds are
config dials if a chattier world is ever wanted for its own sake
(off-distribution emission at newly-legal states — census first).
