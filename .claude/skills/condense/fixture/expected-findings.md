# Expected findings — the answer key

Three planted losses in `condensed-planted.md` against `original.md`
(the F-050 entry verbatim at main fec8961), one per never-cut
category that can be dropped silently. The fact-loss check on the
pair must list exactly these three — no more, no fewer. **Never show
this file to the checker subagent.**

## Plant A — dropped owner ruling (never-cut category 1)

The owner's density caveat is gone: "under a floor, welfare depends
on beam access, so the welfare numbers here do not transfer across
beam count, lifetime, reach or roster size (owner's caveat,
2026-09-20), and floor-0 welfare priors are not comparators for a
floored world." Not recoverable from the condensed text or the files
it points to by name (the caveat governs how THIS entry's numbers
may be used). The nearby scope facts (one density; count read next,
F-051) were kept, so the checker must notice the caveat itself, not
just the sentence.

## Plant B — dropped reproduction reasoning (never-cut category 2)

The mechanism paragraph is stubbed to "under a floor only a beam
clears the need, so the gradient keeps the walk". Dropped: a nap is
a six-tick minimum ending once the need clears; these minds begin
naps at need 7–9; so on flat ground a beam's higher rate has nothing
to relieve and the walk is worth zero ticks, which PPO correctly
unlearns (F-047, tier 2); under a floor a ground nap leaves the need
at the floor. This is the delegated design reasoning the owner named
2026-09-24: without it a fresh session cannot reproduce why floors
10–25 were the arms, or predict where the effect breaks (the
nap-start need band).

## Plant C — silently changed number (never-cut category 4)

"a frozen mind pays 1.4–5.2" became "1.4–4.2". A compression that
touches a digit is not a compression.

## Not planted — legitimate moves, none should be listed

- The tier 2 comparator sentence merged into the arms sentence;
  conducted-share prose tightened; both keep every number.
- "One beam density only" moved into **Scope** with its F-051
  pointer intact.
- All headline numbers, scope, invalidated-by, re-verify, revisit,
  and promotion text (including the #390 ruling and its reasoning)
  are verbatim or number-exact.
- A finding outside these three is a checker bug or a real slip in
  the condense — investigate before touching the plants.
